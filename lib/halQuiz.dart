import 'package:flutter/material.dart';

import './quiz.dart';

class halQuiz extends StatelessWidget {
  // const halQuiz({ Key? key }) : super(key: key);

  int questionIndex;
  var questions ;

  Function mainquiz(){
   
  }

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}